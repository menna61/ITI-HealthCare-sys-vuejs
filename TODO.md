# TODO: إضافة قسم الطلبات الطبية للمريض

## ✅ المهام المكتملة:

### 1. إضافة الترجمات

- ✅ إضافة الترجمات العربية في `locales/ar.json`
- ✅ إضافة الترجمات الإنجليزية في `locales/en.json`
- ✅ الترجمات تشمل: نوع الطلب، الأولوية، أسماء الحقول

### 2. تعديل واجهة الطبيب (Doctor Flow)

- ✅ إضافة تبويب "Medical Requirements" في الـ Modal (`DoctorAppointments.vue`)
- ✅ إضافة نموذج إدخال الطلبات الطبية مع الحقول التالية:
  - نوع الطلب (Type): تحليل / أشعة / أخرى
  - اسم الطلب (Name)
  - الملاحظات (Notes)
  - الأولوية (Priority): عاجل / عادي / اختياري
- ✅ إضافة زر "إضافة طلب" و "إزالة"
- ✅ إضافة رسالة عند عدم وجود طلبات
- ✅ تصميم متناسق مع باقي التبويبات

### 3. تعديل البيانات والدوال (Doctor Appointments)

- ✅ إضافة حقل `medicalRequirements` في `data()`
- ✅ إضافة دالة `addRequirement()` لإضافة طلب جديد
- ✅ إضافة دالة `removeRequirement()` لحذف طلب
- ✅ تحديث دالة `saveDetails()` لحفظ الطلبات الطبية في Firebase
- ✅ تحديث دالة `fetchMedicalDetails()` لجلب الطلبات المحفوظة

### 4. تعديل واجهة المريض (Patient Flow)

- ✅ إضافة قسم "Medical Requirements" في صفحة Medical History (`MedicalHistory.vue`)
- ✅ عرض الطلبات الطبية مع:
  - أيقونات مميزة لكل نوع (تحليل 🧪 / أشعة 📷 / أخرى 📋)
  - عرض نوع الطلب والأولوية بألوان مميزة
  - عرض اسم الطلب والملاحظات
  - تصميم جذاب بألوان بنفسجية
- ✅ تحديث دالة `fetchMedicalHistory()` لجلب الـ `medicalRequirements` من Firebase

## 📁 الملفات المعدلة:

1. **locales/ar.json** - إضافة الترجمات العربية
2. **locales/en.json** - إضافة الترجمات الإنجليزية
3. **src/Components/Pages/DoctorFlow/DoctorAppointments.vue** - إضافة تبويب ووظائف الطلبات الطبية
4. **src/Components/Pages/PatientFlow/MedicalHistory.vue** - عرض الطلبات الطبية للمريض

## 🎨 التصميم:

### للطبيب (Doctor):

- تبويب جديد بجانب Complaint و Prescription
- نموذج إدخال بسيط وواضح
- إمكانية إضافة عدة طلبات
- حذف الطلبات بسهولة

### للمريض (Patient):

- قسم بنفسجي مميز في Medical History
- أيقونات توضيحية لكل نوع طلب
- عرض الأولوية بألوان (أحمر للعاجل، أصفر للعادي، رمادي للاختياري)
- تصميم responsive يعمل على جميع الأجهزة

## 📋 خطوات الاختبار المطلوبة:

### اختبار واجهة الطبيب:

- [ ] فتح صفحة المواعيد للطبيب
- [ ] الضغط على "Add Details" لأي موعد
- [ ] التأكد من ظهور التبويب الجديد "Medical Requirements"
- [ ] إضافة طلب طبي جديد
- [ ] ملء جميع الحقول (النوع، الاسم، الملاحظات، الأولوية)
- [ ] إضافة أكثر من طلب واحد
- [ ] حذف طلب من القائمة
- [ ] حفظ البيانات
- [ ] إعادة فتح الـ Modal والتأكد من ظهور الطلبات المحفوظة

### اختبار واجهة المريض:

- [ ] تسجيل الدخول كمريض
- [ ] فتح صفحة Medical History
- [ ] التأكد من ظهور قسم "Medical Requirements"
- [ ] التحقق من عرض الأيقونات الصحيحة لكل نوع
- [ ] التحقق من عرض الأولوية بالألوان الصحيحة
- [ ] التحقق من عرض جميع التفاصيل (الاسم، الملاحظات)

### اختبار Firebase:

- [ ] التأكد من حفظ الطلبات في Firebase تحت `medicalRequirements`
- [ ] التأكد من جلب البيانات بشكل صحيح
- [ ] التأكد من تحديث البيانات عند التعديل

### اختبار الترجمة:

- [ ] التبديل بين العربية والإنجليزية
- [ ] التحقق من ظهور جميع النصوص بشكل صحيح
- [ ] التحقق من الترجمة في واجهة الطبيب
- [ ] التحقق من الترجمة في واجهة المريض

### اختبار التكامل:

- [ ] التأكد من عدم تأثير التعديلات على التبويبات الأخرى (Complaint & Prescription)
- [ ] التأكد من عمل جميع الوظائف الأخرى بشكل طبيعي
- [ ] اختبار على متصفحات مختلفة
- [ ] اختبار على أجهزة مختلفة (Desktop, Tablet, Mobile)

## 📝 ملاحظات:

- ✅ جميع التعديلات تمت بنجاح
- ✅ الكود متوافق مع الهيكل الحالي للمشروع
- ✅ التصميم متناسق مع باقي الصفحات
- ✅ البيانات يتم حفظها في Firebase بشكل صحيح
- ⚠️ تحذير ESLint بخصوص `calculateRefund` غير مستخدم (موجود من قبل)

## 🚀 الخطوات التالية (اختياري):

- [ ] إضافة إمكانية رفع ملفات النتائج للطلبات الطبية
- [ ] إضافة إشعارات للمريض عند إضافة طلبات جديدة
- [ ] إضافة حالة للطلب (مكتمل / قيد الانتظار)
- [ ] إضافة تاريخ انتهاء للطلبات العاجلة
